let base = 8;
let altura = 10;

function getTheTriangle (base, altura){
    let area = (base * altura)/2
    console.log("A área do triângulo é " + area.toFixed(2) + "cm²")
}

getTheTriangle(base, altura)